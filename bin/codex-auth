#!/usr/bin/env bash
set -euo pipefail

project_dir="${CODEX_AUTH_PROJECT:-"$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"}"
export PYTHONPATH="$project_dir${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m codex_auth "$@"
